// theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#2E294E', // Your primary color
        },
        secondary: {
            main: '#F72676', // Your secondary color
        },
    },
});

export default theme;
